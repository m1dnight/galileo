{
  "name": "galileo-app",
  "version": "1.0.0",
  "description": "Galileo",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "make": "npm run build && electron-forge make"
  },
  "author": "Jane Doe",
  "license": "MIT",
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/maker-dmg": "^7.11.2",
    "electron": "^43.2.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "lib-galileo": "github:m1dnight/lib-galileo",
    "yaml": "^2.9.0"
  }
}
